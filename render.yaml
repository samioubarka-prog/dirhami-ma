services:
  - type: web
    name: dirhami-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: DATABASE_URL
        value: sqlite:///dirhami.db
      - key: SECRET_KEY
        generateValue: true
